# Beach House Studios

> **Recent Update**: The site has been upgraded to the "Mindful" Design System with a Square Color Harmony.

A high-performance, design-forward website for **The Beach House LA**, a hybrid analog/digital recording studio. Built with **Astro**, **Tailwind CSS**, and **Netlify**.

## 🎨 Design System
See [docs/DESIGN_SYSTEM.md](docs/DESIGN_SYSTEM.md) for the source of truth on:
*   **Square Harmony Palette** (Amber, Indigo, Emerald, Rose)
*   **Typography** (Instrument Serif + Inter)
*   **Component patterns** (MindfulCard, Premium Buttons)

## 🚀 Quick Start

```bash
# Install dependencies
npm install

# Start development server
npm run dev
# -> http://localhost:4321
```

## 🏗️ Project Structure
*   `src/components`: Contains all UI components (`MindfulCard`, `Hero`, `GearList`, etc.).
*   `src/layouts`: Global page layouts (`Layout.astro`).
*   `src/styles`: Global CSS and Tailwind `@theme` configuration.
*   `src/data`: JSON data for site content (`site-data.json`).

## 🛠️ Key Technologies
*   **Astro**: Static Site Generation for speed.
*   **Tailwind CSS (v4)**: Utility-first styling with `@theme` variables.
*   **Google Fonts**: Optimized loading for Inter and Instrument Serif.

